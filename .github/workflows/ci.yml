name: Integration Tests
on:
  workflow_dispatch:
  pull_request_target:
    branches-ignore: ['llvm-**']
  merge_group:
    branches: [main, 'dev-**']
    types: [checks_requested]
  push:
    branches: [main]
concurrency:
  group: ${{ github.event_name == 'pull_request_target' && format('pr-{0}', github.event.pull_request.number) || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}
permissions: read-all

jobs:

  runner-preparation:
    uses: ./.github/workflows/runner-preparation.yml
    secrets: inherit

  pre-commit:
    uses: ./.github/workflows/pre-commit.yml
    secrets: inherit

  integration-tests-ascend:
    needs: runner-preparation
    if: needs.runner-preparation.outputs.matrix-ASCEND != ''
    uses: ./.github/workflows/integration-tests-ascend.yml
    with:
      matrix: ${{ needs.runner-preparation.outputs.matrix-ASCEND }}
    secrets: inherit
