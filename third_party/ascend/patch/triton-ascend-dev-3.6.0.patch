diff --git a/python/triton/runtime/autotuner.py b/python/triton/runtime/autotuner.py
index 0c4d710496..d98afb86f9 100644
--- a/python/triton/runtime/autotuner.py
+++ b/python/triton/runtime/autotuner.py
@@ -126,7 +126,7 @@ class Autotuner(KernelInterface):
         return self._do_bench
 
     def _bench(self, *args, config, **meta):
-        from ..compiler.errors import CompileTimeAssertionFailure
+        from ..compiler.errors import CompileTimeAssertionFailure, MLIRCompilationError
 
         verbose = knobs.autotuning.print
         if verbose:
@@ -162,7 +162,7 @@ class Autotuner(KernelInterface):
 
         try:
             return self.do_bench(kernel_call, quantiles=(0.5, 0.2, 0.8))
-        except (OutOfResources, CompileTimeAssertionFailure, PTXASError) as e:
+        except (OutOfResources, CompileTimeAssertionFailure, MLIRCompilationError) as e:
             if verbose:
                 print(f"Autotuning failed with {e}")
             return [float("inf"), float("inf"), float("inf")]
diff --git a/python/src/specialize.cc b/python/src/specialize.cc
index 3449e3c90..e104743ae 100644
--- a/python/src/specialize.cc
+++ b/python/src/specialize.cc
@@ -403,8 +403,8 @@ std::pair<py::object, py::object> handle_tuple(PyObject *backend, PyObject *arg,
   for (Py_ssize_t i = 0; i < size; ++i) {
     PyObject *item = PyTuple_GET_ITEM(arg, i); // Borrowed reference
-    // python reference calls specialize recursively with default arguments set
-    // currently this is is_const=False, specialize_value=True, align=True
-    auto [type, key] = specialize_arg(backend, item, false, true, true);
+    // Tuple elements keep the historical is_const=false and
+    // specialize_value=true defaults while propagating the alignment policy.
+    auto [type, key] = specialize_arg(backend, item, false, true, align);
     if (!type || !key)
       return {};
     // Steals reference
diff --git a/python/triton/runtime/jit.py b/python/triton/runtime/jit.py
index 1e07a267f..b2992457f 100644
--- a/python/triton/runtime/jit.py
+++ b/python/triton/runtime/jit.py
@@ -407,7 +407,8 @@ def create_function_from_signature(sig, kparams, backend):
             is_const = kp.is_const
             specialize = not kp.do_not_specialize
             align = not kp.do_not_specialize_on_alignment
-            ret = f"specialize_impl(backend, {name}, {is_const}, {specialize}, {align})"
+            ret = (f"specialize_impl(backend, {name}, {is_const}, {specialize}, "
+                   f"{align} and backend.use_alignment_specialization(options))")
             if kp.annotation_type:
                 if isinstance(kp.annotation_type, str):
                     if kp.annotation_type == "u1" or kp.annotation_type[:2] in ["fp", "bf"]:
