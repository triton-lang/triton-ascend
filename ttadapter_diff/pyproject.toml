[project]
name = "ttadapter-diff"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "cloudpickle>=3.1.2",
    "joblib>=1.5.3",
    "pebble>=5.2.0",
    "psutil>=7.2.2",
]

[project.scripts]
ttadapter-diff = "ttadapter_diff:main"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"
